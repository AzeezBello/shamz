import type {Metadata} from "next"; import Image from "next/image"; import Link from "next/link"; import {notFound} from "next/navigation"; import {Navbar} from "@/components/navbar"; import {portfolio} from "@/data/portfolio";
export function generateStaticParams(){return portfolio.map(p=>({slug:p.slug}))}
export async function generateMetadata({params}:{params:Promise<{slug:string}>}):Promise<Metadata>{const {slug}=await params;const p=portfolio.find(x=>x.slug===slug);if(!p)return{title:"Project not found — ShamzPhoto"};return{title:`${p.title} — ShamzPhoto`,description:`${p.category} photography by ShamzPhoto — ${p.location}, ${p.year}.`}}
export default async function Project({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const p=portfolio.find(x=>x.slug===slug);if(!p)notFound();return <main><Navbar/><div className="px-6 pt-28 pb-6 lg:px-10"><Link href="/portfolio" className="text-xs uppercase tracking-[.2em]">← Portfolio</Link></div><div className="mx-auto max-w-[1400px] px-6 pb-24 lg:px-10"><div className="relative aspect-[16/9] overflow-hidden"><Image src={p.image} alt={p.title} fill priority sizes="100vw" className="object-cover"/></div><div className="grid gap-10 py-16 md:grid-cols-[1fr_2fr]"><p className="text-xs uppercase tracking-[.2em] text-white/40">{p.category} · {p.location} · {p.year}</p><div><h1 className="display text-6xl sm:text-8xl">{p.title}</h1><p className="mt-8 max-w-2xl leading-8 text-white/55">A visual story created with an editorial eye, natural moments and an emphasis on atmosphere.</p></div></div></div></main>}
