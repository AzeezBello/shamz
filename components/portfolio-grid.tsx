import Image from "next/image"; import Link from "next/link"; import {portfolio} from "@/data/portfolio";
export function PortfolioGrid(){return <div className="grid gap-x-5 gap-y-12 sm:grid-cols-2">{portfolio.map((p,i)=><Link key={p.slug} href={`/portfolio/${p.slug}`} className={i%3===0?"group sm:pt-16":"group"}><div className="relative aspect-[4/5] overflow-hidden bg-white/5"><Image src={p.image} alt={p.title} fill sizes="(max-width:640px) 100vw,50vw" className="object-cover transition duration-700 group-hover:scale-[1.035]"/></div><div className="mt-4 flex justify-between border-b border-white/10 pb-4"><h3 className="display text-2xl">{p.title}</h3><span className="text-[10px] uppercase tracking-[.2em] text-white/40">{p.category}</span></div></Link>)}</div>}
